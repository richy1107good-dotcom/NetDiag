# NetDiag
A simple network diagnostic CLI written in C#

Note: When running inside GitHub Codespaces or other cloud container environments, ICMP Echo (Ping) may be blocked. In such cases, the program may report TimedOut even though DNS resolution and HTTP connectivity are functioning normally.
